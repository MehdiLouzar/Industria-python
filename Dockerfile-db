# Dockerfile-db - simplified PostGIS image
FROM postgis/postgis:14-3.3

# Copy the SQL script executed at container start-up
COPY db/init/initDB.sql /docker-entrypoint-initdb.d/01-populate-data.sql

# The PostGIS image automatically creates the database, enables the extension
# and runs scripts located in /docker-entrypoint-initdb.d

